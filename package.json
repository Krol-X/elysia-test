{
  "name": "test",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "build": "bun build ./src/index.ts --compile --minify --outfile ./stream-api",
    "start": "./stream-api",
    "db:init": "touch app.db"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.4.2",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.ts"
}
